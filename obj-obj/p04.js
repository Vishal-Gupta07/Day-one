//if you want to find value of a key (key) no need to run loop;

//if you have a value and you want to know  that value is in  which key?  then run a loop;


let obj={
  name:"vishal",
  age:24,
  shcool:"DSA",
  food:"samosh",
}

for(key in obj){
  console.log(obj);
}